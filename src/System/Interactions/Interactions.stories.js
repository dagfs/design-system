import React from "react";

import { storiesOf } from "@storybook/react";

storiesOf("System", module).add("Interactions", () => (
  <div>
    <h1>
      How we interact with the system is importat. This page list the
      interaction models of our site.
    </h1>
  </div>
));
